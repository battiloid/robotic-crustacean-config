spring:
  application:
    name: book-service
  redis:
    host: localhost
    port: 6379
    
server:
  port: 8083

management:
    security:
        sessions: never
  
eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 5
